// When check icon is clicked :

//Change icon to "ok" 
$("#checkicon-task-<%=@task.id%>").attr('class', 'glyphicon glyphicon-ok check-icon disable');
//Change row to class to "success" 
$("#row-task-<%=@task.id%>").attr('class', 'task-completed success');
//Updates progress bar 
//$("#progressbar-<%=@project.id%>").attr('style', 'width: <%=@project.progress%>%')
//$("#progressbar-<%=@project.id%>").text('<%=@project.progress%>%')
<%= render 'shared/update_progress', project: @project %>
